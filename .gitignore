# Virtual environments
.venv/
env/
venv/
ENV/

# Python bytecode & cache
__pycache__/
*.py[cod]
*$py.class
*.so
.pytest_cache/

# User data & sensitive files (never commit API keys or private speech history)
.env
config.json
history.json

# Downloaded model weights (downloaded on demand, >1.5 GB)
models/
*.onnx
*.bin

# IDE & agent scratch dirs
.claude/
.idea/
.vscode/
.agents/
*.html
AGENTS.md
CLAUDE.md
GEMINI.md

# Internal working notes: build plans, risk register, UI implementation specs.
# Never published -- docs/ holds everything meant for readers of this repo.
local-docs/

# Build & distribution
build/
dist/
dist-installer/
*.spec
!infinisper.spec
*.egg-info/

# Logs
*.log
logs/

# Voice recordings made with benchmarks/record_clips.py -- your own voice, kept local by default.
benchmarks/clips/

# Screen recording the README GIF was made from (28 MB); the GIF in assets/ is what ships
demo.mp4
